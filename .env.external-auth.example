# External auth example environment variables

# Third-party validation endpoint (POST { cookie: string })
EXTERNAL_AUTH_URL=

# Name of the cookie to extract from incoming requests
EXTERNAL_AUTH_COOKIE_NAME=external_auth_cookie

# Optional: API key to call the third-party validation service
EXTERNAL_AUTH_API_KEY=
